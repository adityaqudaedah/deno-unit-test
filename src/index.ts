import { fibo_recursion as fib_recursive} from "./lib/fibo_recursion.ts"
import { fibo_for_loop as fib_for_loop } from "./lib/fibo_for_loop.ts"
import { fibo_while_loop as fib_while_loop } from "./lib/fibo_while_loop.ts"

export const fibo_recursion = fib_recursive
export const fibo_for_loop = fib_for_loop
export const fibo_while_loop = fib_while_loop